# masterwave-frontend
# Navegue para a pasta do seu frontend cd /caminho/para/masterwave-ai # Inicie o git, adicione os ficheiros, e faça o primeiro commit git init git add . git commit -m "Initial frontend code" # Ligue ao seu repositório GitHub e envie o código git remote add origin <URL_DO_SEU_REPO_FRONTEND_NO_GITHUB> git branch -M main git push -u origin main
